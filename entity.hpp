#ifndef ENTITY_H
#define ENTITY_H

class Entity {
    int maxHP, curHP;
    int maxMP, curMP;
    int maxSTR, curSTR;
    int maxTOU, curTOU;
    int maxATT, curATT;
    int maxDEF, curDEF;
};

#endif
